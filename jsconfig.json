{
    "compilerOptions": {
        "baseUrl": "./", // Base directory for resolving paths
        "paths": {
            "@/*": [
                "src/*"
            ],
        },
    },
    "include": [
        "src/**/*"
    ], // Includes all source files in your project
    "exclude": [
        "node_modules"
    ] // Exclude dependencies
}